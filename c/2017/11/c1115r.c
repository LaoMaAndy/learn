/*
function: recurse, bit operation
*/
#define NEED_STRING
#include "0head.c"

void prnd(int n);
void prnstr(const char *s, int i);
int fact(int n);
unsigned bitset(unsigned v, int n);
unsigned bitclr(unsigned v, int n);
int bitchk(unsigned v, int n);
unsigned getbits(unsigned v, int p, int n);
