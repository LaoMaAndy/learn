#!/usr/bin/env python3
r'''
# 简单语句 Simple statements
    简单语句由一个单独的逻辑行构成。 多条简单语句可以存在于同一行内并以分号分隔。 
    简单语句的句法为:
    simple_stmt ::=  expression_stmt
                     | assert_stmt
                     | assignment_stmt
                     | augmented_assignment_stmt
                     | annotated_assignment_stmt
                     | pass_stmt
                     | del_stmt
                     | return_stmt
                     | yield_stmt
                     | raise_stmt
                     | break_stmt
                     | continue_stmt
                     | import_stmt
                     | future_stmt
                     | global_stmt
                     | nonlocal_stmt

# 表达式语句 Expression statements
# 赋值语句 Expression statements
# 增强赋值语句 Augmented assignment statements
# 带标注的赋值语句 Annotated assignment statements
# assert 语句  The assert statement
# pass 语句 The pass statement
# del 语句 The del statement
# return 语句 The return statement
# yield 语句 The yield statement
# raise 语句 The raise statement
# break 语句 The break statement
# continue 语句 The continue statement
# import 语句 The import statement
## future 语句 Future statements
# global 语句 The global statement
# nonlocal 语句 The nonlocal statement



'''


if __name__ == '__main__':
    print(__doc__)
    def test():
        print('ref_simple_statement.py')
    test()
